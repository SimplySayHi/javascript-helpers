
export const defaultOptions = {
    cache:          'no-store',
    credentials:    'same-origin',
    headers: {
                    'Content-Type': 'application/json'
    },
    method:         'GET',
    mode:           'same-origin',
    redirect:       'follow',
    timeout:        0,
    url:            location.href
}
