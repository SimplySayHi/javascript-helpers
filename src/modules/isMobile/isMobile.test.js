
import isMobile from './isMobile';

describe( 'Check if Mobile', () => {

    test( 'isMobile', () => {
        const expectTest = isMobile;
        expect( expectTest ).toBe( expectTest );
    } );

} );
